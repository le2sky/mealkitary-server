comment:
    behavior: default
    require_changes: false
    branches:
        - develop
        - main

coverage:
    status:
        patch: off
        project:
            default:
                target: auto
                threshold: 70%
